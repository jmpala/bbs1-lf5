'''
Erstelle eine Funktion spritkosten(a,b,c). Die Funktion benötigt die gefahrenen Kilometer (a), den Spritverbrauch (b) und die Kosten pro Liter (c).
Für die Kosten pro Liter kannst du das Internet aufsuchen. Als Ergebnis liefert die Funktion die Kosten für die gefahrene Strecke. Lagere die Funktion
in eine eigene .pyDatei aus und importiere sie anschließend in dein Hauptprogramm. Der Rechner kann entsprechend der Bedürfnisse beliebig erweitert werden.
'''

import k6a2funktionen as func

print(func.spritkosten(10, 5, 1));

